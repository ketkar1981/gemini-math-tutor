{
  "name": "Gemini Math Tutor",
  "image": "mcr.microsoft.com/devcontainers/python:3.11-bullseye",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python", 
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "editor.formatOnSave": true
      }
    }
  },
  "postCreateCommand": "bash -lc \"echo '[postCreateCommand] ✅ Container created at $(date)' | tee /tmp/postCreate.log\"",
  "postStartCommand": "bash -lc \"echo '[postStartCommand] 🚀 Container started at $(date)' | tee -a /tmp/postCreate.log\"",
  "postAttachCommand": "bash -lc \"echo '[postAttachCommand] 💻 VS Code attached at $(date)' | tee -a /tmp/postCreate.log\""
}

